from setuptools import setup, find_packages

setup(
    name="SNAL",         # your package name
    version="0.1.0",           # version
    packages=find_packages(),  # automatically find sub-packages
    install_requires=[],       # dependencies (e.g. ["numpy", "astropy"])
)
